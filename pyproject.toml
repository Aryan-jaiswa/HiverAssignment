[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "hiver-assignment"
version = "0.1.0"
description = "AppleSupport AI Agent"

[tool.pytest.ini_options]
pythonpath = ["."]
